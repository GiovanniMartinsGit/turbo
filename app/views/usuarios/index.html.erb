<p style="color: green"><%= notice %></p>

<%= form_with url: search_usuarios_path, method: :get, data: {turbo_frame: 'search_usuario', controller: 'search', action: 'keyup->search# search_perform'} do |f| %>
  <%= f.text_field :query, value: params[:query] %>
<% end %>

<h1>Usuarios</h1>

<%= render partial: 'usuarios/datatable', locals: {usuarios: @usuarios} %>

<%= link_to "New usuario", new_usuario_path %>

<div><%= "lorem ipsum" * 50 %></div>

